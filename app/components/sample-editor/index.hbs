<h1 local-class="title">Qalam</h1>

<div local-class="container">
  <div local-class="editor">
    <div
      {{did-insert this.setupEditor this}}
      {{will-destroy this.destroyEditor this}}
    >
    </div>

    <div style="display: none;">
      <h3>
        Hello ProseMirror
      </h3>

      <p>
        This is editable text. You can focus it and start typing.
      </p>

      <p>
        To apply styling, you can select a piece of text and manipulate
        its styling from the menu. The basic schema
        supports <em>emphasis</em>, <strong>strong text</strong>, <a href="http://marijnhaverbeke.nl/blog">links</a>, <span style="text-decoration: underline;">underline</span>, <span style="text-decoration: line-through;">strikethrough</span>, superscripts like a<sup>2</sup> + b<sup>2</sup> = c<sup>2</sup> and <code>code font</code>.
      </p>

      <p>
        Block-level structure can be manipulated with key bindings (try
        ctrl-shift-2 to create a level 2 heading, or enter in an empty
        textblock to exit the parent block), or through the menu.
      </p>

      <p>
        Try using the “list” item in the menu to wrap this paragraph in
        a numbered list.
      </p>

      <table>
        <tbody>
          <tr>
            <th>Firstname</th>
            <th>Lastname</th>
            <th>Age</th>
          </tr>

          <tr>
            <td>Jill</td>
            <td>Smith</td>
            <td>50</td>
          </tr>

          <tr>
            <td>Eve</td>
            <td>Jackson</td>
            <td>94</td>
          </tr>
        </tbody>
      </table>
    </div>

    {{#if this.showImageGallery}}
      <ImageGallery
        @close={{this.closeImageGallery}}
        @insert={{fn this.insertImage this.editorStates}}
      />
    {{/if}}
  </div>


  <pre local-class="output">
  </pre>
</div>

<NotificationContainer @position="bottom-right" />
